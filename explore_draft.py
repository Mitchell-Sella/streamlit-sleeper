import requests

# Use a known public league or user to test?
# Or create dummy response structure if I can't.
# I will use a dummy one if I can't find one, but let's try to query the sleeper API for documentation purposes
# or use the provided link (docs.sleeper.com) as reference.
# The user provided the docs link. I should read the docs.
# Or I can use 'view_text_website' on the docs.

print("Checking Sleeper API docs for Draft Picks...")
